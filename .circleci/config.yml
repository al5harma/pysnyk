version : 2.1
orbs:
    build:
        docker:
            - image: cimg/python:3.10.2
        steps:
        - checkout
        - run: npm install
        - snyk/scan:
            fail-on-issues: true
            monitor-on-build: true
            severity-threshold: high
            token-variable: SNYK_TOKEN
        
workflows:
    version: 2
    build_and_test:
        jobs:
        - build